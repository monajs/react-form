/**
 * context
 * author: yangxi
 */

import React from 'react'

// form data context
export const FormDataContext = React.createContext(null)

// verify context
export const FormVerifyContext = React.createContext(null)
